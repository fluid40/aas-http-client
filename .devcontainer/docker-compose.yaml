services:
  dev_container:
    build: .
    container_name: python_dev_container
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
    networks:
      - bridge

networks:
  bridge:
